/**
 * The build stamp, replaced at build time by Vite's `define` — see
 * `vite.config.ts`. Declared rather than imported because `define` performs a
 * textual substitution: there is no module to import it from.
 */
declare const __APP_VERSION__: string;
