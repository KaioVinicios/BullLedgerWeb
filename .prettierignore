dist/
node_modules/
bun.lock
docs/
openapi.yaml
src/types/api.d.ts

# Vendored from upstream and updated wholesale — reformatting locally would
# conflict on every sync.
.claude/
.impeccable/

# Git-ignored agent scratch: briefs, reports, and review packages. Prettier
# does not read .gitignore, so it would otherwise check files no commit holds.
.superpowers/
